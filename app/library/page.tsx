import { ComingSoon } from "@/components/shell/ComingSoon";

export default function LibraryPage() {
  return (
    <ComingSoon
      name="Library"
      blurb="Every post that has gone out, plus the templates and assets behind them. Searchable, reusable."
    />
  );
}
